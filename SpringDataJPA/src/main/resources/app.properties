spring.config.import=optional:file:.env[.properties],optional:file:./SpringDataJPA/.env[.properties]

# Db connection
spring.datasource.url=${DB_URL:jdbc:sqlite:spring_data_jpa.db}
spring.datasource.driver-class-name=org.sqlite.JDBC

# Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.hibernate.naming.physical-strategy=org.hibernate.boot.model.naming.PhysicalNamingStrategyStandardImpl
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.community.dialect.SQLiteDialect

springdoc.swagger-ui.tagsSorter=alpha
springdoc.swagger-ui.operationsSorter=alpha

# @Value ile okumak mantıklı
db.username=stawiz
db.password=5T@wi22026

# @ConfigurationProperties ile okumak mantıklı
app.servers[0].ip = 192.168.10.2
app.servers[0].location = Germany

app.servers[1].ip = 192.168.10.3
app.servers[1].location = Belgium

app.servers[2].ip = 192.168.10.4
app.servers[2].location = USD